{
  "builds": [
    {
      "src": "app.py",
      "use": "@vercel/python",
      "config": {
        "distDir": "app"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "app.py"
    }
  ],
  "scripts": {
    "build": "bash build.sh"
  }
}
